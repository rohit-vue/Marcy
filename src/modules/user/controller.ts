import type { FastifyInstance } from "fastify";

/**
 * Placeholder for future user-facing HTTP routes (dashboard, billing, etc.).
 */
export async function registerUserHttpRoutes(_app: FastifyInstance): Promise<void> {
  /* intentionally empty */
}
